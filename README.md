# skill-selector

**Skill 选择与管理元技能（Meta-Skill）**——让 AI 自己管理自己的 Skill。

当一个项目积累了大量 Skill 后，AI 该怎么选？该不该新建？旧的要不要删？skill-selector 给了 AI 一套完整的决策框架。

## 核心能力

- **智能匹配**：P0-P4 五级优先级算法，不再盲目试错
- **精简去重**：三级覆盖度阈值（≥95% 实质等同 / 90-95% 灰色地带 / <90% 各有特色）
- **废弃检测**：8 种触发条件，含大模型过时淘汰机制
- **互斥校验**：5 种互斥类型自动检测
- **自主创建**：对话中检测重复模式，主动建议封装新 Skill
- **Token 优化**：Bootstrap → Cache 两层架构，后续调用节省约 95% token

## 三条设计铁律

1. **项目优先** — 跟当前项目有关的优先
2. **效率优先** — Token 最省 > 执行最快 > 效率最高 > 功能完整 > 通用性强
3. **全局保护** — 全局 Skill 可能被其他项目依赖，不误删

## 快速开始

### 安装（30 秒）

将 `skill-selector` 目录复制到全局 Skills 目录：

```
~/.agents/skills/skill-selector/
├── SKILL.md              # 核心元技能
└── profiles/
    └── _template.md      # 项目缓存模板
```

复制即用，无需配置环境变量或安装依赖。

### 使用

1. **首次使用**：在新项目中触发 skill-selector 时，自动完成 Bootstrap —— 扫描全部 Skill、去重/互斥检测、Token 评估、生成项目缓存
2. **日常使用**：读缓存（~200 tokens），秒级响应
3. **手动刷新**：对 AI 说"刷新 skill 缓存"

## 效果

| 指标 | 使用前 | 使用后 |
|------|--------|--------|
| 每次选择 Skill Token 消耗 | ~5000 | ~200（缓存命中） |
| 选择准确率 | 盲目试错 | 一步到位 |
| 废弃 Skill 处理 | 静默占用 | 自动检测+推荐清理 |
| 新建 Skill 依据 | 凭感觉 | 三层评估模型 |

## 文件结构

```
skill-selector/
├── SKILL.md                    # 核心元技能（13 个章节）
│   ├── § 核心设计原则
│   ├── § Bootstrap → Cache 架构
│   ├── § P0-P4 匹配算法
│   ├── § 操作优先级 / 创建标准
│   ├── § Token 优化 / 精简去重
│   ├── § 废弃检测 / 互斥检测
│   ├── § 自主创建 / 生命周期
│   └── ...
└── profiles/
    └── _template.md            # 项目缓存模板
```

## 适合谁用

任何拥有 **5 个以上 Skill** 的 SOLO 项目/工作流。

## License

MIT
